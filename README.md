# Drag_Drop_items
A webpage with 2divs providing draging and dropping functionality from one div to another div with a visual feedback after dropping with a reset button which will reset both the divs to its original state.
